# JavMetaLite v0.5.0-rc1 人工验收

这份清单验证 v0.5 新增功能与既有保存流程的最终集成。v0.4 已通过的 JAVLibrary、目标影片冲突、覆盖保护和回滚用例没有发生功能改动，并由自动化回归继续覆盖，因此无需全部重跑。

## 验收原则

- 只使用普通影片副本，不选择唯一原件。
- 建议使用 LibreDMM 与 R18.dev 都能找到、且两边字段或封套明显不同的番号。
- 在独立目录准备两份相同影片副本，例如：

```text
JavMetaLite-v0.5-RC1-Test/
  R05-Save/
  R06-Jellyfin/
```

- 测试前分别记录两个影片副本的 SHA-256。
- 如果影片哈希改变、目标影片被覆盖或取消预览后仍产生文件，立即停止并保留现场。

## R01 — RC1 启动与冻结界面

1. 启动 `publish-v0.5.0-rc1\JavMetaLite.exe`。
2. 确认底部显示 `v0.5.0-rc1 · 发布候选版`。
3. 确认默认来源为“多来源搜索（推荐）”。
4. 确认 NFO、高清海报和横版 fanart 默认开启；全部剧照、直接覆盖、整理文件夹和影片重命名默认关闭。
5. 观察左侧两个预览框。

期望：软件正常启动；搜索前没有封套等待文字；poster 与 fanart 之间保留舒适间距。

## R02 — 多来源搜索与字段候选

1. 选择测试影片并保持“多来源搜索（推荐）”。
2. 搜索资料。
3. 打开标题、导演、演员、类型和简介的来源菜单。
4. 将其中一个有双来源候选的字段切换至 R18.dev，再切回 LibreDMM。

期望：

- 状态栏显示已取得 `LibreDMM + R18.dev`，没有异常弹窗。
- LibreDMM 有值时默认使用日文资料；空字段可由 R18.dev 补全。
- 候选菜单完整深色，显示来源、值预览和当前项勾选。
- 每次只改变当前字段，其他字段保持不变。

## R03 — 手动值恢复

1. 手动修改一个有来源候选的字段，例如导演。
2. 确认来源变成“手动编辑”。
3. 从候选菜单切回网站来源。
4. 再次打开菜单并恢复刚才的手动值。

期望：网站值和最近一次手动值都能恢复，且不会改变其他字段。

## R04 — 封套与 Fanart 统一来源

1. 确认左侧“封套 / Fanart”来源标记位于封套上方右侧。
2. 点击来源标记，在 LibreDMM 与 R18.dev 之间切换。
3. 观察 poster、fanart、尺寸文字和右侧 metadata 字段。

期望：

- 下拉菜单为完整深色结构，不出现白色边栏。
- poster 与 fanart 每次同时切换，不会使用不同来源。
- 成功后显示类似 `横板封套：2184×1468`，不显示等待文字。
- 两个预览框在切换前后保持相同间距。
- 切换封套不会改变文字字段，也不会出现剧照来源或跨来源合并选项。

## R05 — 最终选择、预览、取消与原位保存

输入：`R05-Save` 中的影片副本。

1. 选择至少一个非默认文字字段候选，并选择一个明确的封套来源。
2. 开启 NFO、海报、fanart 和全部剧照；保持直接覆盖及两项整理功能关闭。
3. 点击保存，检查预览中的目标路径和输出项目。
4. 第一次取消预览，确认目录没有新增 NFO、图片或临时文件夹。
5. 再次保存并确认执行。
6. 打开 NFO，核对标题等字段是最终选择的值。
7. 打开 poster、fanart 和若干 `extrafanart` 图片。
8. 重新计算影片 SHA-256。

期望：

- 取消预览不会产生文件变化。
- 确认后在影片旁生成有效 NFO、poster、fanart 和剧照。
- poster 与 fanart 来自统一选择的封套来源；剧照仍来自整体搜索结果。
- 影片路径和文件名保持不变，SHA-256 与测试前一致。

## R06 — 整理输出与 Jellyfin 读取

输入：`R06-Jellyfin` 中的另一份影片副本。

1. 完成多来源搜索并选择最终文字与封套来源。
2. 开启“整理到番号文件夹”和“影片重命名为番号”。
3. 保存前检查预览，确认目标结构为：

```text
R06-Jellyfin/
  番号/
    番号.ext
    番号.nfo
    番号-poster.jpg
    番号-fanart.jpg
```

4. 确认保存并重新计算移动后影片的 SHA-256。
5. 把番号目录加入独立 Jellyfin 测试库并扫描。
6. 检查标题、原始标题、简介、演员、类型、poster 和背景 fanart。

期望：影片被安全整理且 SHA-256 不变；Jellyfin 只识别一部影片，并显示软件中最终选择的字段与图片。

## 已由既有验收或自动化覆盖

- JAVLibrary 浏览器辅助导入：v0.4 人工验收已通过，v0.5 未改变该流程。
- 直接覆盖、目标影片冲突、重复番号目录与事务回滚：v0.4 回归套件持续覆盖。
- 多来源单方失败、双方失败、番号不匹配与重复调用：离线核心测试覆盖。
- dev3–dev5 的菜单样式、多来源日志及封套布局：分阶段人工检查均已通过。

## 结果反馈

```text
R01 PASS / FAIL：
R02 PASS / FAIL：
R03 PASS / FAIL：
R04 PASS / FAIL：
R05 PASS / FAIL：
R06 PASS / FAIL：
两个影片 SHA-256 是否一致：YES / NO
```

如果失败，请附对应步骤截图、状态栏文字和与该番号相邻的日志行；不要发送包含其他影片路径的整份日志。
