<Window x:Class="JavMetaLite.App.SavePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存前变更预览"
        Width="900" Height="650" MinWidth="720" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#0D1117"
        ShowInTaskbar="False">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#151B23" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2A3544" />
        <SolidColorBrush x:Key="TextBrush" Color="#F1F5F9" />
        <SolidColorBrush x:Key="MutedBrush" Color="#93A4B8" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#263244" />
            <Setter Property="BorderBrush" Value="#36465D" />
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#4F8CFF" />
            <Setter Property="BorderBrush" Value="#6FA2FF" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="保存前变更预览" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="只有点击“确认并执行”后才会写入 metadata 或移动影片。" Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,18,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="当前影片" Foreground="{StaticResource MutedBrush}" />
                <TextBox x:Name="SourcePathTextBox" Grid.Column="1" IsReadOnly="True" BorderThickness="0" Background="Transparent" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" />
                <TextBlock Grid.Row="1" Text="保存结果" Foreground="{StaticResource MutedBrush}" Margin="0,10,0,0" />
                <TextBox x:Name="TargetPathTextBox" Grid.Row="1" Grid.Column="1" IsReadOnly="True" BorderThickness="0" Background="Transparent" Foreground="#72E3A6" TextWrapping="Wrap" Margin="0,10,0,0" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="6">
            <ListView x:Name="ChangesList" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="IsHitTestVisible" Value="False" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="8,7">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="125" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Background="{Binding BadgeBackground}" CornerRadius="6" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Top">
                                <TextBlock Text="{Binding Action}" Foreground="{Binding BadgeForeground}" FontSize="11" FontWeight="SemiBold" />
                            </Border>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Description}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Path}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,3,0,0" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Border x:Name="WarningPanel" Grid.Row="3" Background="#332615" BorderBrush="#79551D" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,14,0,0" Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="WarningText" Foreground="#FFD18A" TextWrapping="Wrap" />
                <CheckBox x:Name="OverwriteConfirmCheckBox" Content="我确认覆盖上面列出的已有 metadata 文件" Margin="0,10,0,0" Checked="OverwriteConfirm_Changed" Unchecked="OverwriteConfirm_Changed" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="4" Margin="0,18,0,0">
            <TextBlock x:Name="SafetyText" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Text="影片目标冲突时软件不会执行保存。" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" IsCancel="True" />
                <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}" Content="确认并执行" Click="Confirm_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
