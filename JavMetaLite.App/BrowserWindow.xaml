<Window x:Class="JavMetaLite.App.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="{DynamicResource Browser.Title}"
        Width="1050" Height="760" MinWidth="800" MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border x:Name="BrowserToolbar" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource Browser.Instruction}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" />
                    <TextBlock x:Name="UrlText" Foreground="{StaticResource MutedBrush}" FontFamily="Segoe UI" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
                </StackPanel>
                <Button x:Name="ImportButton" Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="{DynamicResource Browser.ReadCurrent}" Click="Import_Click" />
            </Grid>
        </Border>

        <wv2:WebView2 x:Name="Browser" Grid.Row="1" NavigationCompleted="Browser_NavigationCompleted" />

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,10">
            <TextBlock Text="{DynamicResource Browser.Privacy}" Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
