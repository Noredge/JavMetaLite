# v0.7.0-dev3 人工检查

状态：2026-08-17 用户确认 T01–T05、T08 PASS；T06–T07 因没有 UNC 测试环境记为 SKIP。跨盘符安全复制、取消、冲突保护、同卷回归及 T03 中的影片 SHA-256 检查均通过，dev3 验收完成。

dev3 的自动化门禁会覆盖安全复制、独立 SHA-256 校验、取消、晚到冲突和失败回滚。以下项目聚焦真实 Windows 跨盘符、可选 UNC 路径及 UI 观感。请继续只使用影片副本。

## 准备

1. 在来源盘建立 `Incoming`，在另一个本地盘建立 `Library`；两者必须是不同盘符。
2. 复制一部能识别番号的测试影片到 `Incoming`，最好选择较大的文件以便观察并取消复制。
3. 记录来源影片 SHA-256。
4. 如果有可写网络共享，再准备一个 UNC 根目录；没有真实共享时将 T06、T07 中仅适用于 UNC 的部分记为 `SKIP`，不算失败。

## T01 — UI 调整

- 启动软件并选择影片。
- 检查“影片重命名为番号”位于“保存方式”一行。
- 切换到自定义根目录，检查“选择文件夹”和右侧“保存”之间留有清楚间隔。
- 在正常窗口和最小支持宽度下检查控件不重叠。

预期：两项布局调整清晰，目标路径区域仍然完整可读。

## T02 — 跨盘符预览

- 选择另一个本地盘的 `Library`。
- 分别切换重命名开关，确认最终路径。
- 点击保存进入预览但先取消。

预期：界面显示“安全复制 + SHA-256 校验”；保存可用；预览中影片动作是“复制并校验”，最终影片和 metadata 路径全部正确；取消预览后磁盘零改动。

## T03 — 跨盘符真实执行

- 再次保存并确认执行。
- 观察状态栏的复制百分比、SHA-256 校验和来源移除阶段。
- 完成后重新计算目标影片 SHA-256。

预期：目标影片和 metadata 完整存在；来源影片只在成功后消失；目标 SHA-256 与准备阶段一致；来源和目标均无 `.JavMetaLite-*.tmp` 残留。

## T04 — 复制中取消

- 用一份新的较大影片副本重新测试跨盘符保存。
- 在复制进度出现后点击“取消当前操作”。

预期：显示已取消；来源影片仍存在且 SHA-256 不变；目标没有最终影片或本次 metadata，也没有事务临时目录残留。若文件太小来不及点击，可换更大副本重试。

## T05 — 目标影片冲突

- 在计算出的最终影片路径预先放置一个同名测试文件。
- 重新载入来源影片并尝试保存。

预期：预览或保存前明确阻止；既不覆盖目标文件，也不移动或删除来源影片。

## T06 — 可用 UNC 共享（可选）

- 在自定义根目录粘贴一个真实、可写的 UNC 路径并执行保存。
- 对比来源与目标 SHA-256。

预期：行为与 T03 相同，最终影片与 metadata 可正常读取，来源仅在成功后消失。没有可用 UNC 时记为 `SKIP`。

## T07 — 不可用或不可写目标

- 使用一份新的来源副本，将目标设为不可用 UNC 或无写入权限的目录并保存。

预期：出现可理解的失败信息；来源影片与原 sidecar 均保持原位；没有可见的半成品最终影片。无法安全构造权限故障时可只测试不可用 UNC。

## T08 — 同卷回归

- 在来源盘建立同卷自定义 `Library`，用另一份影片副本完成一次保存。

预期：仍使用普通安全保存流程而非复制进度；结果正确，影片 SHA-256 一致，无临时文件残留。

## 回报格式

```text
T01 PASS/FAIL
T02 PASS/FAIL
T03 PASS/FAIL
T04 PASS/FAIL
T05 PASS/FAIL
T06 PASS/FAIL/SKIP
T07 PASS/FAIL/SKIP
T08 PASS/FAIL
跨盘目标 SHA-256 是否一致：YES/NO
UNC 目标 SHA-256 是否一致：YES/NO/SKIP
```

如有保存或恢复问题，请不要手动删除 `.JavMetaLite-*.tmp`，先点击“打开日志”并提供最新日志末尾；该目录可能包含恢复所需备份。
