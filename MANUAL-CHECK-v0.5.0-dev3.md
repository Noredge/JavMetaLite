# v0.5.0-dev3 manual check

本轮只检查字段候选菜单，不需要保存影片或生成 metadata。测试前复制一份普通测试影片；不要使用唯一原片。

## D01 — 多来源候选

1. 启动 `JavMetaLite.exe`，选择测试影片。
2. 保持“自动补全（推荐）”，搜索一个 LibreDMM 与 R18.dev 都能找到、且自动补全确实调用了两个来源的番号。
3. 找到带 `▾` 的来源标记并点击。

期望：

- 菜单是深色且文字清晰。
- 每个候选同时显示来源名称和值预览。
- 当前候选前有 `✓`。
- 只有一个候选的来源标记不带 `▾`，点击不会弹出空菜单。

## D02 — 只切换单个字段

1. 记录标题、导演和演员的当前内容。
2. 在“标题”的候选菜单中改选另一个来源。

期望：

- 标题立即变为所选候选。
- 标题右侧的来源名称同步更新。
- 导演、演员和其他字段没有随整份资料一起切换。

## D03 — 手动值与来源值互相恢复

1. 手动修改一个原本有来源的字段，例如导演。
2. 确认标记变成“手动编辑 ▾”。
3. 从菜单切回 LibreDMM 或 R18.dev。
4. 再次打开菜单，切回“手动编辑”。

期望：

- 来源原值可以恢复。
- 最近一次手动值仍在菜单中，也可以再次恢复。
- 每次切换只影响该字段。

## D04 — 兼容性冒烟

1. 不保存，重新搜索或重新选择一次影片。
2. 确认旧影片的候选不会残留到新影片。
3. 按原有流程打开保存预览，但在预览中取消。

期望：

- 新搜索建立自己的来源标记与候选。
- 保存预览内容正常，取消后没有文件变化。
- 程序没有异常弹窗或退出。

结果可按 `D01 PASS / FAIL` 至 `D04 PASS / FAIL` 回复；若失败，请附上字段名称、所选来源和截图。

## 实际结果（2026-08-15）

- D01 FAIL：候选菜单左侧仍显示系统白色栏，整体并非完全深色。
- D02 PASS
- D03 PASS
- D04 PASS

D01 的显示问题在 `v0.5.0-dev3-r1` 中修复，复测只需重新执行 D01。
