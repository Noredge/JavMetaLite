# JavMetaLite v0.8.1-dev1 简短人工检查

状态：2026-08-29 用户确认 P01–P03 全部 PASS；开启、关闭和取消记忆三种重启路径均符合预期。

本轮只检查偏好在真实程序重启后的可见状态，不需要搜索资料、选择影片或执行保存。

## P01 — 记住开启状态

1. 启动 `publish-v0.8.1-dev1\JavMetaLite.exe`。
2. 勾选“记住保存偏好”和“直接保存并覆盖（跳过预览）”，关闭程序后重新启动。
3. 确认两个选项均为勾选状态，状态栏明确提示直接覆盖已开启。

通过标准：用户明确记住的直接覆盖状态能够跨重启恢复。

## P02 — 记住关闭状态

1. 保持“记住保存偏好”开启，取消勾选“直接保存并覆盖（跳过预览）”。
2. 关闭程序后重新启动。

通过标准：偏好记忆仍开启，直接覆盖保持关闭。

## P03 — 取消偏好记忆

1. 再次勾选直接覆盖，然后取消勾选“记住保存偏好”。
2. 关闭程序后重新启动。

通过标准：两个选项均恢复为关闭；先前保存的其他偏好也回到默认值。

## 结果反馈

```text
P01 PASS/FAIL
P02 PASS/FAIL
P03 PASS/FAIL
补充观察：
```
