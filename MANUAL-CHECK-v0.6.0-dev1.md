# v0.6.0-dev1 检查说明

## 结论

本阶段不需要功能性人工验收。

dev1 只建立本地 sidecar 定位和安全 NFO 只读解析能力，尚未接入主界面，也没有加入任何新的保存、移动或覆盖路径。日常操作应继续表现为 v0.5.0 的既有流程。

## 已由自动化覆盖

- 大小写不敏感地定位同名 NFO、poster 和 fanart。
- 正确读取当前支持的影片字段、多值导演/类型/演员和演员图片 URL。
- 缺失字段产生诊断，但不会阻止读取其他有效字段。
- 原始 XML 的未知元素、未知属性、注释与空白可供后续阶段完整保留。
- 读取前后所有测试影片、NFO 与图片的文件数量和字节内容完全一致。
- 损坏 XML、错误根元素、超大文件、DTD 与外部实体均被安全拒绝。
- v0.4 文件安全回归和 v0.5 WPF 界面 smoke 没有退化。

## 可选启动检查

如果希望确认包本身可运行，只需打开 `publish-v0.6.0-dev1/JavMetaLite.exe`，确认窗口底部显示 `v0.6.0-dev1 · 本地 NFO 只读基座`。不需要使用真实影片测试；界面载入本地 NFO 将在 dev2 实现。
