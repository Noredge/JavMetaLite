# v0.6.0-dev2 增量人工检查

## 准备

请只使用测试副本，不要直接使用唯一一份影片或 NFO。准备两个独立的测试目录：

1. `L01-Local`：一个影片和一个与影片同名、可正常读取的 Jellyfin 影片 NFO。
2. `L04-Clean`：另一个没有同名 NFO 的影片。

例如影片为 `SNOS-255.mp4`，NFO 必须命名为 `SNOS-255.nfo`。dev2 不读取本地图片；poster/fanart 将在 dev3 接入。

## L01 — 自动载入本地 NFO

1. 运行 `publish-v0.6.0-dev2/JavMetaLite.exe`。
2. 选择 `L01-Local` 中的影片。
3. 检查 NFO 中已有的标题、日期、演员、简介等字段是否自动出现。
4. 检查这些非空字段右上角是否显示 `本地 NFO`。
5. 检查底部状态栏是否显示完整 NFO 路径和“只读检查”。
6. 检查“保存”按钮是否禁用，并在鼠标停留时说明 dev2 的只读边界。

通过标准：资料与 NFO 一致，来源和路径清楚，无法在 dev2 覆盖该 NFO。

## L02 — 在线资料只增加候选

1. 保持 L01 影片，点击“搜索资料”，使用“多来源搜索（推荐）”。
2. 记录搜索前已有的本地标题；确认搜索后标题没有被自动改成在线标题。
3. 如果本地 NFO 有空白字段而网站有值，确认空白字段得到补齐。
4. 打开标题等来源下拉，确认可以看到 `本地 NFO`、`LibreDMM` 和 `R18.dev` 的实际候选；某个来源没有该字段时可以不出现。
5. 分别切换一两个字段，确认只改变所选字段。

通过标准：本地非空值仍是默认，在线结果可检查、可选择，来源之间不会整页互相覆盖。

## L03 — 手动值可恢复

1. 手动修改一个已有多个来源候选的字段，例如标题。
2. 确认来源变成 `手动编辑`。
3. 从下拉切回 `本地 NFO` 或在线来源。
4. 再次打开下拉并切回 `手动编辑`。

通过标准：最近一次手动值可以恢复。

## L04 — 换片后清空旧候选

1. 选择 `L04-Clean` 中没有 NFO 的另一个影片。
2. 检查上一部影片的标题、简介和来源标记都已消失。
3. 检查“保存”按钮恢复可用。
4. 可正常搜索该影片，确认 v0.5 的普通新片流程仍然工作。

通过标准：没有任何上一部影片的本地、在线或手动候选残留。

## L05 — 无效 NFO 安全失败

1. 在第三个测试副本旁创建同名 NFO，内容可简单写成 `<tvshow><title>Invalid</title></tvshow>`。
2. 选择该影片。
3. 检查状态栏是否明确说明无法安全读取且原文件未修改。
4. 检查字段没有载入错误内容，“保存”按钮保持禁用。
5. 用文本编辑器重新打开该 NFO，确认内容仍与测试前完全一致。

通过标准：无弹窗循环、无自动重建、无覆盖，错误可理解且原文件不变。

## 回报格式

```text
L01 PASS / FAIL：
L02 PASS / FAIL：
L03 PASS / FAIL：
L04 PASS / FAIL：
L05 PASS / FAIL：
补充观察：
```
