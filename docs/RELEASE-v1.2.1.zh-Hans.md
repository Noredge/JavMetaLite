# 1.2.1 推送与交付清单

## 当前状态

- 用户已确认 `1.2.1-preview.2` 本地验收无问题。
- 已准备本地正式版本号 `1.2.1`，本记录不表示已提交、推送、打标签或发布。
- 发布正文：[1.2.1 版本说明](RELEASE-NOTES-v1.2.1.md)。实现与预览验证历史：[补丁记录](PATCH-v1.2.1.zh-Hans.md)。

## 独立复查（2026-09-07）

由当前维护者重新逐项审查差异，不是另一位审查者的背书。未发现本轮阻断问题。

- 预览、覆盖检查、暂存写入和最终提交共享输出文件名；目录级 NFO 不因普通单片原地保存而改为另一套文件。
- 读取回退受同一逻辑影片检查约束，CD 分组按已有规则识别。并不扩展为支持所有第三方 NFO 字段。
- PNG 编码与扩展名一致；WebP 原地替换有明确阻止与四语言说明。
- 空剧照目录只在整个事务成功后清理，使用非递归删除，并跳过链接目录；并发出现新文件会使删除失败而非递归移除。
- 清理矩阵覆盖单片与 CD 分段各 7 个场景；权限拒绝、链接及竞态分支经代码审查，未声称逐个实机故障注入。
- 未增加依赖、下载并发、来源策略或库迁移设置；旧截图继续标注原版本，不冒充本版截图。

## 本地正式包验证

2026-09-07：正式身份 `1.2.1` / `1.2.1.0` 已通过核心测试 46/46、文件事务回归 54/54、完整 WPF UI 自动化。四语言 README 与发布材料的 66 个相对链接已检查，差异空白检查通过。新增的已跟踪文本未命中本机用户名路径、媒体盘路径或常见 GitHub 令牌模式；新增文档和测试文件另经人工阅读。该检查不等于通用秘密扫描器。

本地文件：`release/JavMetaLite-v1.2.1-win-x64-portable.zip`；ZIP 清单、EXE 身份、README、许可证、第三方声明及 SHA-256 校验通过。

SHA-256：`4529C773AB154BE7BAC7B3A513EA4E9B52D622552ADF59E8B179B520529D8369`。

完整运行记录保留在本地 `artifacts/release121-review-gate.log`，不提交。包的校验不是打包 EXE 的交互式实机验收；用户验收的是 preview.2，本轮业务实现保持不变，仅定稿版本身份与材料。

## 拟提交清单

1. 补丁源码、回归与 UI 测试、四语言文案、正式版本号。
2. 四语言 README、CHANGELOG、本版说明与交付记录。
3. 此前已要求整理的维护路线及两份历史优化材料入口，建议独立文档提交。

建议提交标题：`Fix legacy folder sidecar compatibility and still cleanup`；文档提交：`Document 1.2.1 release and maintenance workflow`。

不提交：`release/`、`artifacts/`、SDK/缓存、日志、真实影片或 NFO、私人截图、旧开发分支历史。不要直接使用 `git add .`；按文件清单检查后暂存。

## 推送与发布门槛

- [ ] 获得推送授权后，核对远端 main 是否变化，再提交并普通推送；不强推。
- [ ] 等待该提交的 Windows CI 成功。现有工作流已运行完整自动化、新增回归、打包、便携包校验和 artifact 上传，无需修改。
- [ ] 下载并核验该次 CI 的实际 ZIP、EXE 版本与校验文件；不能用本地包校验结果代替 CI 产物验证。
- [ ] 获得标签与 Release 发布授权后，将 `v1.2.1` 指向已验证提交。
- [ ] Release 标题使用 `JavMetaLite v1.2.1`；正文使用本版说明；上传 CI 的 portable ZIP、对应 `.sha256` 和 `SHA256SUMS.txt`，保留 v1.2.0。

2026-09-07：用户已授权正式推送 main，已成功刷新远端。标签与 Release 仍待单独授权；实际推送和 CI 状态以 GitHub 对应提交为准。
