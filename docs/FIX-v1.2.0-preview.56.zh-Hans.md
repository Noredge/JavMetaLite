# preview.56 最终审查修复与验证

日期：2026-09-05。内部回验版本，不是正式发布。仅修复最终审查中已确认的问题，不追加功能或重构。

状态更新：用户已于 2026-09-05 确认测试完毕、可以定稿，preview.56 针对性回验完成。当前进入 1.2.0 本地定稿，见 [定稿记录](FINAL-v1.2.0.zh-Hans.md)。下方测试、截图和包身份保留 preview.56 当时的事实，不用于证明新 1.2.0 包已通过验证或获准发布。

## 修复范围

- 本地样张保留：50 张上限只计算在线选择，全部本地选择按原顺序参与保存，仍保留既有内容去重；预览和执行使用同一计数规则。本地读取失败中止事务，不能静默跳过后再覆盖原图。
- 批次冲突：确认前检查不同影片计划之间的输出／待移除路径冲突，提示使用独立番号文件夹。保留合法多 CD 分组与原有 Jellyfin 文件命名。
- 异步状态：查看器应用来源后的预览纳入 busy／取消流程，拒绝迟到回写；单片重试的分辨率比较与资料应用使用同一取消／错误处理范围。
- 交付说明：随包第三方说明包含实际依赖的版权、许可及 notices，仍为四文件便携包；打包回归拒绝不完整声明和版本不匹配。
- 可读性：图片标题与操作按钮分行，长移除按钮允许换行，目标位置选择框按文字调整宽度；简繁“合并 CD1/CD2”改为“分组”，历史验收语句明确时间。

## 验证记录

**审查列出的 5 项代码问题与 1 项许可材料缺口均已修复，完整自动化及回验包校验通过。** 这不代替真实网站、系统 DPI 或用户媒体副本上的针对性回验。

| 验证 | 结果 |
| --- | --- |
| 新版本完整门禁 | Core 46/46、文件事务 43/43、WPF 与发布脚本 PASS |
| 原图保护回归 | 旧实现 3/3 失败；修复后 3/3 通过，含单片／多 CD、51 本地 + 60 在线、迟于在线上限的本地选择、短暂读锁 |
| UI／批次冲突回归 | 实际查看器应用和取消、迟到结果与缓存隔离、取消后再次重试；共享 Extra Fanart、待移除／保留文件依赖、NFO 覆盖授权与跳过预览均不能绕过冲突；独立目录与合法多 CD 不误阻 |
| 专项重复 | UI 修复／可读性与本地样张组各再跑 3 轮，全部通过 |
| 四语言可读性 | 930／1120 逻辑宽度的实际文字测量通过；10 张新合成截图逐张目视核对 |
| 许可回归 | 缺正文、摘要替代、重复正文、版权缺失、版本不符、未知依赖和旧构建清单均被拒绝 |
| 本地 ZIP 独立解压 | 4 个预期文件、EXE 版本、暂存／解压 EXE 哈希、ZIP 两份校验和、README 版本与许可正文均一致 |

可复用命令：

```powershell
.\scripts\Test-Automated.ps1 -DotNet ./.dotnet-sdk/dotnet.exe -Configuration Release -NoRestore -BuildOutputDirectory bin/final-fix56/
```

代码证据：[本地样张测试](../JavMetaLite.RegressionTests/LocalSampleSafetyRegressionTests.cs)、[UI 和批次测试](../JavMetaLite.UiSmokeTests/FinalReviewFixTests.cs)、[可读性测试](../JavMetaLite.UiSmokeTests/ReadabilityCloseoutTests.cs)、[许可门禁](../scripts/Test-ReleasePackage.ps1)。

工作区证据（`artifacts/` 被 Git 忽略，仅本地有效）：完整日志（`artifacts/final-fix56/final-gate.log`）、包校验（`artifacts/final-fix56/portable-validation.json`）、阅读材料链接检查（`artifacts/final-fix56/document-validation.json`）。重复日志为 `artifacts/final-fix56/repeat-ui-{1,2,3}.log` 与 `repeat-save-{1,2,3}.log`。旧失败复现在 `artifacts/final-review-20260905/` 保留。

## 当前本地回验包

- 版本：`1.2.0-preview.56`；文件版本：`1.2.0.0`。
- ZIP：`release/JavMetaLite-v1.2.0-preview.56-win-x64-portable.zip`，60,472,915 字节。
- ZIP SHA-256：`6CDDE19A17F94643154951C12DCE6659ACC90068AC312B725EACF2993F4893C3`。
- EXE SHA-256：`DCD8E5D89FCF1A4C35841C9448E54263916B512A0A10FAED42E3B72366C3AB18`。
- 四文件仍为 EXE、README、项目 LICENSE、第三方说明；第三方正文 86,885 字节。
- 本轮自动化记录不包含包内 EXE 的人工启动验收，WPF 门禁运行的是测试构建。用户后续确认测试完毕，但未逐项说明覆盖；不能把两者视为同一项验证。
- 更新 [四语言与查看器截图](SCREENSHOTS-v1.2.0.md)，短 README 仍各 47 行。

本轮测试使用合成影片、图片及假 HTTP，不操作真实媒体、不压测来源网站、不清理旧恢复目录。未扩大为跨影片并行保存、依赖升级或新的持久化架构。

## 用户针对性回验

2026-09-05，用户确认测试完毕、可以定稿，已记录为 preview.56 针对性回验完成。用户未逐项列出测试结果，不据此补记不同 DPI、全部异常场景或新 1.2.0 包内 EXE 的人工启动通过。

当时建议用少量可恢复副本检查：普通追加样张；共享目录批量预览的冲突提示及改用番号文件夹后的保存；查看器切换来源后切片；取消来源重试后再次搜索；英文／日文默认窗口文字。保留这些项目作为覆盖参考，不要求重复整轮验收。

无需在真实影片上人为制造锁、断电或大量失败。原图超过上限与短暂占用由隔离回归覆盖。

## 授权边界

未提交、打标签、推送或创建对外 Release。旧 preview.55 包和恢复目录保留；本轮回验包已重新构建并校验，不能将旧包改名为正式版。用户已认可针对性回验并同意 1.2.0 本地定稿；新版构建、独立校验与后续授权以定稿记录为准。
