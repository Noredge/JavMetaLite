# JavMetaLite v0.8.0-dev3 简短人工检查

使用一个测试影片和两个真实存在的空文件夹 A、B；本轮不需要点击保存。

## H01 — 最近目录选择

1. 勾选“记住保存偏好”，目标位置选择“自定义根目录的番号文件夹”。
2. 依次通过“选择文件夹”选择 A、B。
3. 打开“最近目录”，选择 A。

预期：菜单显示 2 条深色记录；选择 A 后路径框立即切回 A。

## H02 — 不可用路径

1. 在路径框输入一个确认不存在的绝对路径，例如 `C:\JavMetaLite-dev3-missing-root`，然后按 Enter。

预期：提示“当前不可用”且说明不会自动创建，保存按钮不可用；该文件夹没有被创建。

## H03 — 移除与清空

1. 从“最近目录”重新选择 A，再打开菜单并选择“移除当前记录”。
2. 确认路径框仍为 A，然后选择“清空最近记录”。
3. 关闭并重新启动程序。

预期：当前路径仍为 A；“最近目录”没有记录并处于不可点击状态，清空的记录不会重新出现。

## 回报格式

```text
H01 PASS/FAIL
H02 PASS/FAIL
H03 PASS/FAIL
```

配置迁移、最多 5 条、重复路径去重和未来版本保护已由自动化测试覆盖。

## 验收结果

2026-08-29：

```text
H01 PASS
H02 PASS
H03 PASS
```

dev3 人工检查全部通过。最近目录选择、不可用路径的零创建保护、单条移除、清空历史及重启后的状态均符合预期，可以进入 v0.8.0 RC。
