# JavMetaLite v0.8.0 路线：日常使用效率

## 目标

在不扩大单片编辑器范围的前提下，减少每次启动与选择目标位置时的重复操作：

```text
从 Windows 打开一个影片
→ JavMetaLite 直接载入同一条现有编辑流程
→ 恢复用户明确允许记忆的安全偏好
→ 搜索、检查、预览并保存
```

不增加文件关联安装器、媒体库扫描、批量刮削、自动搜索、命名规则引擎或新的资料来源。

## dev1 — 启动参数直接载入影片

状态：已完成；E01–E08 人工检查与影片 SHA-256 验证全部通过。

- 支持 `JavMetaLite.exe "D:\Movies\ABC-123.mp4"`，也支持 Windows“打开方式”传入的单个影片路径。
- 启动路径成功后复用选择按钮与拖入影片的同一套载入流程，包括番号、本地 NFO、poster/fanart、来源候选与目标路径刷新。
- 启动只载入本地影片，不会自动联网搜索，也不会写入、移动或重命名影片及 metadata；诊断日志仍按现有规则记录。
- 启动参数必须恰好包含一个真实存在的受支持影片；空路径、多文件、文件夹、不存在路径及不支持格式会明确提示并写入日志，主窗口保持可用。
- 把支持的影片扩展名和文件选择器过滤条件集中到 Core，避免选择、拖入和启动参数采用不同规则。
- 加入 Core 参数解析覆盖与 WPF 真实载入链 smoke 覆盖。

dev1 不注册 Windows 文件关联；便携版用户可以自行通过“打开方式”选择 JavMetaLite。

人工验收确认无参数启动、命令行、Windows“打开方式”、不存在路径、文件夹、不支持格式、多影片参数和既有入口均符合预期；仅载入影片不会生成或修改 metadata，影片 SHA-256 保持一致。

## dev2 — 安全偏好持久化

状态：已完成；P01–P02 简短人工检查全部通过。

- 建立带版本号的本地 JSON 配置，使用临时文件与原子替换保存。
- 只记忆用户明确允许的目标位置、重命名和 metadata 输出偏好。
- “直接保存并覆盖（跳过预览）”永远不持久化，每次启动保持关闭。
- 损坏或来自未来版本的配置不会阻止启动，并可恢复安全默认值。
- 配置位于当前 Windows 用户的 `%LOCALAPPDATA%\JavMetaLite\settings.json`；未勾选“记住保存偏好”时不会保留该文件。
- 损坏配置、未来版本保护和原子写入由离线自动化测试覆盖，人工只检查真实重启后的可见行为。

人工验收确认安全偏好可以按用户选择跨重启恢复，取消记忆后会回到安全默认值，并且“直接保存并覆盖”始终不会跨启动保留。

## dev3 — 最近目标根目录

状态：已完成；H01–H03 简短人工检查全部通过。

- 为自定义根目录提供简洁的最近使用记录。
- 失效、本地离线或 UNC 暂不可用的路径只提示，不自动创建或写入。
- 用户可以移除单条记录或清空全部记录。
- 不增加命名模板、媒体库配置档案或多层规则界面。
- 最多保留 5 条，统一放在现有路径框旁的深色“最近目录”菜单；当前路径与历史记录相互独立。
- dev2 的 schema v1 配置会迁移到 schema v2；未来版本配置仍保持只读保护。
- 清空历史不会清空当前路径，也不会在程序关闭时把它自动加回；重新选择或确认输入后才再次记录。

人工验收确认深色最近目录菜单、目录切换、不可用路径提示、零目录创建、单条移除、完整清空及重启后的持久状态均符合预期。

## RC — 日常工作流验收

- 命令行、Windows“打开方式”、拖入与选择按钮得到一致的影片状态。
- 配置重启、损坏配置和离线目标均安全回退。
- 直接覆盖始终默认关闭，所有写入仍经过现有预览与事务保护。
- v0.7 的来源选择、本地 NFO、同卷/跨卷整理、回滚与影片 SHA-256 回归全部通过。

## v0.8.0 完成标准

- 用户可以从 Windows 直接把单个影片交给 JavMetaLite。
- 日常安全偏好可以显式记忆，同时危险选项不会跨启动保留。
- 新入口不复制编辑或保存实现，不改变单片定位，也不增加后台行为。
- 完整自动化门禁与分阶段人工检查全部通过。
