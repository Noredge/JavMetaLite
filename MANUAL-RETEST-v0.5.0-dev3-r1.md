# v0.5.0-dev3-r1 manual retest

本轮只复测 dev3 的 D01；D02、D03、D04 已通过，不需要重复。

## R01 — 完整深色候选菜单

1. 启动 `v0.5.0-dev3-r1` 的 `JavMetaLite.exe`。
2. 选择测试影片，以“自动补全（推荐）”搜索资料。
3. 依次点击几个带 `▾` 的字段来源标记。
4. 移动鼠标，在不同候选之间切换高亮。

期望：

- 菜单从最左侧到最右侧都是深色背景，不再出现白色边栏或白色勾选区。
- 来源名称、值预览和当前项的 `✓` 均清晰可读。
- 鼠标悬停项使用蓝色深色高亮，未悬停区域仍为深色。
- 长标题或简介可以换行，不会把菜单撑出屏幕。

结果回复 `R01 PASS` 或 `R01 FAIL`；若失败，请附候选菜单截图。
