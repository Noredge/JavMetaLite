# v0.6.0-dev3 增量人工检查

## 本轮范围

本轮只检查新增的本地图片与手动完整封套流程。dev2 的 L01–L05 已全部通过，不需要重复；已有 NFO 仍为只读，往返保存将在 dev4 开放。

请只使用影片与图片的测试副本。运行：

```text
publish-v0.6.0-dev3\JavMetaLite.exe
```

准备三个独立目录：

1. `A01-LocalPair`：`SNOS-255.mp4`、`SNOS-255.nfo`、`SNOS-255-poster.jpg`、`SNOS-255-fanart.jpg`。
2. `A02-Partial`：`IPZZ-850.mp4`，以及 `IPZZ-850-poster.jpg` 或 `IPZZ-850-fanart.jpg`，只保留其中一张。
3. `A04-Manual`：一个没有同名 NFO/poster/fanart 的影片，以及另一处的一张真实完整横板封套。

扩展名也可使用 `.jpeg`、`.png` 或 `.webp`；文件基本名和影片必须完全一致。

## A01 — 自动发现本地 poster + fanart

1. 选择 `A01-LocalPair\SNOS-255.mp4`。
2. 检查左侧 poster 与 fanart 是否分别显示已有本地图片，而不是重新裁切后的同一张图。
3. 检查封套区域右上角是否显示 `本地图片 ▾`。
4. 检查横板图下方是否显示 `横板封套：宽×高`，尺寸应与本地 fanart 一致。
5. 打开封套来源菜单，确认 `本地图片` 的说明为“现有 poster + fanart（成对来源）”。
6. 检查本地 NFO 字段仍正常载入，“保存”仍因 dev4 尚未开放而禁用。

通过标准：两张本地图各自原样预览、来源清楚、NFO 行为不变。

## A02 — 缺少一张时不伪造

1. 选择 `A02-Partial` 中的影片。
2. 如果目录中只有 poster：确认 poster 正常显示，fanart 区域保持空白；菜单说明显示 `fanart 缺失`。
3. 如果目录中只有 fanart：确认 fanart 正常显示，poster 区域保持拖入提示；菜单说明显示 `poster 缺失`。
4. 不要求同时测试两个方向，任选当前准备的一种即可。

通过标准：存在的一张正常预览，缺失的一张没有从另一张自动生成。

## A03 — 在线搜索只增加封套候选

1. 回到 A01 影片并点击“搜索资料”，使用“多来源搜索（推荐）”。
2. 搜索完成后，确认左侧仍显示原本的两张本地图片，来源仍是 `本地图片 ▾`。
3. 打开来源菜单，确认除了 `本地图片`，还出现成功返回封套的 LibreDMM/R18.dev 候选。
4. 切换到任意在线来源，确认 poster 与 fanart 一起变化。
5. 再切回 `本地图片`，确认两张已有 sidecar 分别恢复。

通过标准：本地图片保持默认；在线来源可选；poster/fanart 仍只有一个共同来源菜单。

## A04 — 手动选择完整封套并实际输出

1. 选择 `A04-Manual` 中没有任何 sidecar 的影片。
2. 点击封套区域右上角的 `选择封套…`，再选 `选择本地完整封套…`。
3. 在 Windows 文件选择器中选择准备好的真实完整横板封套。
4. 确认来源变为 `手动封套 ▾`，poster 显示封套右半部分，fanart 显示完整横板封套，尺寸文字正确。
5. 保持“生成 NFO”“高清海报”“横版 fanart”勾选；“保存全部剧照”可关闭。
6. 保存前在 PowerShell 记录影片哈希：

   ```powershell
   Get-FileHash "完整影片路径" -Algorithm SHA256
   ```

7. 点击“保存”，检查变更预览后确认执行。
8. 确认影片旁生成同名 `.nfo`、`-poster.jpg`、`-fanart.jpg`；人工查看两张输出的裁切与画质。
9. 再次执行相同的 `Get-FileHash`，确认影片 SHA-256 完全一致。
10. 确认原始手动封套文件仍存在且未被修改。

通过标准：原生文件选择器正常；一张完整封套统一驱动预览和输出；图片质量合理；影片与源封套未被改写。

## A05 — 损坏图片不阻断 NFO

1. 复制 A01 目录到新测试目录。
2. 用记事本新建任意文本内容，并保存覆盖为同名 `SNOS-255-poster.jpg`；保留有效的 NFO 和 fanart。
3. 选择该影片。
4. 确认本地 NFO 字段仍正常出现，有效 fanart 仍可预览，poster 区域保持空白。
5. 检查状态栏是否说明有无效图片被忽略；点击“打开日志”，确认可见 `本地 poster 无效` 记录。
6. 确认原 NFO、影片和有效 fanart 没有被修改。

通过标准：损坏 poster 被独立隔离并可诊断，不妨碍 NFO 与有效 fanart。

## 回报格式

```text
A01 PASS / FAIL：
A02 PASS / FAIL：
A03 PASS / FAIL：
A04 PASS / FAIL：
A05 PASS / FAIL：
影片 SHA-256 是否一致：YES / NO
补充观察：
```
