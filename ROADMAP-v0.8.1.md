# JavMetaLite v0.8.1 路线：完整保存偏好

## 目标

让用户明确启用“记住保存偏好”后，程序完整恢复其保存方式，包括“直接保存并覆盖（跳过预览）”。该选择的风险继续由同一复选框与现有说明表达，不增加第二次确认。

## dev1 — 直接覆盖偏好持久化

- `AppPreferences` 升级到 schema v3，并加入 `DirectSaveOverwrite`。
- 同时启用偏好记忆与直接覆盖时，关闭程序会原子保存该状态，下次启动恢复为开启。
- 关闭直接覆盖后再次保存偏好，下次启动恢复为关闭。
- 取消偏好记忆会清除配置；首次运行、缺失配置、损坏配置和未来版本配置仍使用关闭状态。
- schema v1/v2 配置没有直接覆盖授权，迁移到 v3 时保持关闭。
- 恢复直接覆盖时在状态栏和日志中明确说明，但不增加弹窗，不改变预览、覆盖范围、影片保护、事务或回滚。

## 完成标准

- Core 与 WPF 测试覆盖开启、关闭、安全默认值和 v1/v2 迁移。
- 完整自动化门禁通过。
- 三项真实重启检查全部通过。
- 影片与 metadata 保存流程除偏好恢复外没有行为变化。
