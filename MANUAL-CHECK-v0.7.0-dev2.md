# v0.7.0-dev2 人工检查

状态：2026-08-17 用户确认 D01–D08 全部 PASS，影片 SHA-256 一致。

dev2 的自动化门禁已经覆盖三种模式、路径校验、同卷执行、跨盘符/UNC 阻止、保存预览和影片 SHA-256。以下项目只检查真实 Windows 文件夹选择、界面可读性与一次真实文件工作流。

## 准备

1. 在一个空间足够的本地盘建立测试根目录，例如 `<测试根>\JavMetaLite-dev2`。
2. 建立 `Incoming` 与 `Library` 两个子文件夹，确保它们在同一盘符。
3. 把一部测试影片复制到 `Incoming`，不要使用唯一原件。测试文件名应能识别番号，例如 `下载名 @ SNOS-255-UC.mp4`。
4. 保存测试影片的 SHA-256。
5. 为避免来源网站状态影响目标路径测试，可以使用已经取得资料的影片，或只保留“生成 NFO”并手动填写最少资料。

## D01 — 安全默认与布局

- 启动软件，不选择影片。
- “目标位置”默认应为“影片所在位置（不移动）”。
- “影片重命名为番号”默认关闭。
- 自定义根目录输入框应隐藏。
- 底部应显示“选择影片后显示最终路径”，控件在正常窗口和最小窗口宽度下均清晰、不重叠。

预期：PASS。

## D02 — 来源位置的番号文件夹

- 选择测试影片并确认识别到番号。
- 把目标位置改为“来源位置的番号文件夹”，保持影片重命名关闭。
- 检查行内最终路径，然后点击保存进入预览，但先取消。

预期：最终影片为 `Incoming\SNOS-255\原影片名.mp4`；保存预览列出相同绝对路径；取消后磁盘零改动，影片 SHA-256 不变。

## D03 — 自定义同卷根目录与文件夹选择器

- 选择“自定义根目录的番号文件夹”。
- 点击“选择文件夹”，选择同盘的 `Library`。
- 保持影片重命名关闭并检查最终路径。

预期：输入框显示所选绝对路径；最终影片为 `Library\SNOS-255\原影片名.mp4`；保存按钮可用。

## D04 — 不重复番号目录

- 在 `Library` 下预先建立 `SNOS-255`。
- 用“选择文件夹”直接选择 `Library\SNOS-255`。

预期：最终路径仍为 `Library\SNOS-255\原影片名.mp4`，不会出现 `SNOS-255\SNOS-255`。

## D05 — 重命名保持独立

- 保持自定义根目录模式，分别勾选和取消“影片重命名为番号”。

预期：勾选后最终文件名为 `SNOS-255.mp4`；取消后恢复原影片文件名；目标根目录不改变。

## D06 — 同卷真实执行

- 选择 `Library` 作为自定义根目录，按需要勾选影片重命名。
- 点击保存，检查“当前影片”“最终影片”及每个 NFO/图片目标路径，然后确认执行。
- 完成后重新计算目标影片 SHA-256。

预期：影片和所选 metadata 位于 `Library\SNOS-255`；来源影片已移动；目标影片 SHA-256 与准备阶段一致；没有 `.JavMetaLite-*.tmp` 残留。

## D07 — 跨盘符与 UNC 暂时阻止

- 再选择一份测试影片。
- 在自定义根目录框粘贴另一个盘符的绝对路径。
- 再尝试粘贴一个测试 UNC 文本，例如 `\\example.invalid\Media`；不需要真实连接该地址。

预期：界面分别显示“跨盘符”或“网络路径”尚未开放；保存按钮禁用并带有相同原因；不会访问或创建目标文件。

## D08 — 会话内保留选择

- 恢复一个有效的同卷自定义根目录。
- 重新选择另一部影片。

预期：目标模式和自定义根目录仍保留，最终路径按新番号立即更新。关闭并重新启动软件后，安全默认仍是“影片所在位置（不移动）”。

## 回报格式

```text
D01 PASS/FAIL
D02 PASS/FAIL
D03 PASS/FAIL
D04 PASS/FAIL
D05 PASS/FAIL
D06 PASS/FAIL
D07 PASS/FAIL
D08 PASS/FAIL
影片 SHA-256 是否一致：YES/NO
```

如有界面问题，请附一张包含底部目标位置区域的截图；如有保存问题，请同时点击“打开日志”并提供最新日志末尾。
