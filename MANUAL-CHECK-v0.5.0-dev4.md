# v0.5.0-dev4 manual check

本轮检查真实网站下的多来源搜索闭环。测试不要求保存影片；建议使用一份普通测试影片，不要使用唯一原片。

## S01 — 推荐模式同时取得两个来源

1. 启动 `JavMetaLite.exe` 并选择测试影片。
2. 保持默认的“多来源搜索（推荐）”。
3. 搜索一个 LibreDMM 与 R18.dev 都能找到的番号。

期望：

- 搜索期间显示“正在同时获取 LibreDMM 与 R18.dev…”。
- 完成状态同时列出 `LibreDMM + R18.dev`。
- 标题等两个网站都有值的字段带 `▾`，点击后能看到两个来源的候选。
- 不出现异常弹窗。

## S02 — 默认选择与逐字段候选

1. 打开标题、导演、演员、类型和简介的候选菜单。
2. 对比默认字段和两个来源的内容。
3. 任选一个字段切换至 R18.dev，再切回 LibreDMM。

期望：

- LibreDMM 有值时默认选中 LibreDMM。
- LibreDMM 为空而 R18.dev 有值时默认选中 R18.dev。
- 切换只改变当前字段，其他字段保持不变。
- 两份来源值都能恢复。

## S03 — 显式单来源模式

1. 来源改为 `LibreDMM`，重新搜索一次。
2. 再改为 `R18.dev`，重新搜索一次。

期望：

- 每次只显示所选来源的数据。
- 未手动编辑时，字段来源标记不带多来源下拉箭头。
- 两种单来源搜索都保持 dev3 已通过的编辑与预览行为。

## S04 — 逐来源日志

1. 再执行一次“多来源搜索（推荐）”。
2. 点击“打开日志”，查看当天最新日志。

期望：

- 同一次搜索各有一条 `source=libredmm` 和 `source=r18dev` 的来源结果。
- 成功记录包含 `elapsedMs`、`fields` 与 `screenshots`。
- 日志最后的 metadata 成功记录包含 `reviewSources=2`。
- 没有同一来源在同一次搜索中被无故重复调用的记录。

## S05 — 保存流程兼容性

1. 在多来源结果中切换一个字段。
2. 点击保存并打开变更预览。
3. 确认 NFO 预览使用当前选中的字段值，然后取消保存。

期望：

- 预览使用用户最终选定的字段，而不是强制恢复某个网站的整份资料。
- 取消后影片与 metadata 文件均没有变化。

双方失败、单方失败和番号冲突已经由离线自动化测试覆盖，不要求人为破坏网络复现。

结果可按 `S01 PASS / FAIL` 至 `S05 PASS / FAIL` 回复；若失败，请附状态栏文字、字段名称和相关日志行。
