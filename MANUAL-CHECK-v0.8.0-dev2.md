# JavMetaLite v0.8.0-dev2 简短人工检查

本轮只检查重启后的可见行为，不需要搜索或保存影片。

## P01 — 记住安全偏好

1. 启动程序，勾选“记住保存偏好”。
2. 把目标位置改为“来源位置的番号文件夹”，勾选“影片重命名为番号”和“保存全部剧照”；同时勾选“直接保存并覆盖（跳过预览）”。
3. 关闭并重新启动程序。

预期：目标位置、重命名、全部剧照恢复；“直接保存并覆盖”必须自动关闭。

## P02 — 停止记忆

1. 取消勾选“记住保存偏好”，关闭并重新启动程序。

预期：恢复安全默认值——影片所在目录、不重命名、生成 NFO/海报/fanart、不开启全部剧照，也不开启直接覆盖。

## 回报格式

```text
P01 PASS/FAIL
P02 PASS/FAIL
```

损坏配置、未来版本配置与原子写入已由自动化测试覆盖，不需要人工修改配置文件。

## 验收结果

2026-08-29：

```text
P01 PASS
P02 PASS
```

dev2 人工检查全部通过。明确允许的安全偏好可以跨重启恢复；停止记忆后恢复安全默认值，“直接保存并覆盖”不会跨启动保留，可以进入 dev3。
