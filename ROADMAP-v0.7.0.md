# JavMetaLite v0.7.0 路线：自定义目标路径

## 目标

在保持“轻量单片编辑器”定位的前提下，让用户决定本次影片与 metadata 最终保存到哪里：

```text
选择单个影片
→ 搜索、检查并修改 metadata
→ 选择保存位置与是否重命名影片
→ 在变更预览中确认每一个最终路径
→ 安全写入 NFO、图片与影片
```

不增加媒体库扫描、批量整理、自动目录监控或规则引擎。

## 三种目标模式

1. **影片所在位置**：影片保持在当前文件夹，metadata 写在影片旁边。
2. **来源位置的番号文件夹**：在影片当前文件夹下使用 `<番号>` 文件夹，与 v0.6 的“整理到番号文件夹”一致。
3. **自定义根目录下的番号文件夹**：用户选择媒体库根目录，最终目标为 `<自定义根目录>\<番号>`。

若来源目录或用户选择的自定义根目录已经正好以番号命名，则直接使用该目录，不重复生成 `<番号>\<番号>`。影片是否重命名为标准番号继续作为独立开关，不与目标模式绑定。

## 安全规则

- 自定义根目录必须是绝对路径，可指向本地磁盘、其他盘符或 UNC 网络共享。
- 保存前预览继续显示影片、NFO、poster、fanart 与剧照的最终目标路径。
- 目标影片已存在时始终阻止执行，任何覆盖选项都不能覆盖另一部影片。
- metadata 冲突继续通过现有预览确认或明确的直接覆盖模式处理。
- 目录不存在可以在执行阶段创建；路径被文件占用、路径无效或目标不可写时必须明确失败。
- 跨卷执行必须使用“复制到暂存区 → 校验 SHA-256 → 提交 metadata 与影片 → 成功后删除来源”的事务流程；复制、校验或提交失败时保留来源并清理未完成目标。

## 分阶段路线

### dev1 — 目标路径模型与纯规划

状态：已实现。

- 新增三种明确的目标模式，并兼容 v0.6 的现有布尔选项。
- 抽离不写磁盘的路径规划器，统一计算目标目录、影片名与完整目标路径。
- 校验空白、相对或无效自定义根目录。
- 避免已选番号目录被重复嵌套。
- `BuildPlan` 识别自定义根目录被文件占用以及目标影片冲突。
- 自动化覆盖来源目录、自定义根目录、独立重命名、不同盘符与 UNC 路径。

dev1 不接入主界面，也不开放跨盘/网络执行；它只建立后续 UI 和事务流程共同使用的稳定接口。

### dev2 — 目标位置 UI 与变更预览

状态：已完成；D01–D08 人工检查全部通过。

- 已把原有“整理到番号文件夹”升级为清晰的三选一目标位置控件。
- 仅在选择“自定义根目录”时显示可编辑路径与 Windows“选择文件夹”按钮。
- 本次会话保留所选模式和最后一个有效自定义根目录，新启动仍以“影片所在位置”为安全默认。
- 切换目标模式、路径、影片或重命名开关后立即显示最终影片绝对路径；正式保存预览继续显示全部变更。
- 空路径、相对路径、非法 Windows 目录段以及不支持的目标会显示行内原因并禁用保存。
- 同卷本地目标使用现有事务安全执行；dev2 对跨盘符和 UNC 的暂时阻止已由 dev3 取代。

### dev3 — 跨卷与网络路径安全事务

状态：已完成；T01–T05、T08 人工检查通过，T06–T07 因没有 UNC 环境按计划记为 SKIP。

- 已判断来源与目标是否可直接原子移动；同卷继续使用现有安全移动路径。
- 跨卷或网络目标会复制影片到目标侧暂存区，并分别计算来源与暂存副本 SHA-256。
- 只有校验一致且目标影片与全部 metadata 成功提交后，才移除来源影片与已迁移 sidecar。
- 取消、校验失败、晚到冲突或提交失败会保留来源，恢复被覆盖 metadata，并清理目标侧临时文件；恢复不完整时保留事务目录并给出日志位置。
- 已增加复制/校验进度、状态栏取消、本地目标空间预检，以及成功、取消、哈希不一致、晚到冲突和提交后失败的离线回归测试。
- UI 已把影片重命名开关移到保存方式一行，并拉开目录选择与保存按钮的距离。
- 真实不同盘符保存、复制中取消、目标冲突及同卷回归已通过；UNC 功能由自动化路径/事务测试覆盖，真实共享留到具备环境时补测。

### RC — 真实路径验收与冻结

状态：RC1、RC2 与 RC3 均已通过，`v0.7.0` 已晋升稳定版。

- 本地同卷、自定义不同盘符和可用 UNC 共享各完成一轮真实影片测试。
- 覆盖预览取消、目标冲突、网络中断或等价故障、重试与影片 SHA-256 一致性。
- Jellyfin 扫描自定义目标下的 NFO、poster、fanart 与剧照成功。
- RC 只修复缺陷，不再增加新的整理规则。

当前没有可用 UNC 测试环境，因此真实共享执行不作为 RC1 晋级的阻塞条件：不同盘符真实事务由 dev3 人工测试覆盖，UNC 路径与失败恢复由自动化覆盖；未来具备环境时仍可补充验证实际权限、速度与断线行为。

RC2 不改变整理事务：影片刚载入时仍显示本地 NFO，用户明确搜索成功后改为在线非空文字字段优先、本地字段补空，并保留本地/手动候选；已有本地封套仍保持选中。该集中修正通过后即可晋升稳定版。

RC3 进一步让预览文案与实际文件一致：标准 NFO 只显示“更新 NFO”，检测到额外元素、非标准属性、注释或处理指令时才追加“保留未知 XML”。写入器原有的无损保留规则不变。

RC3 的 P01–P03 与影片 SHA-256 检查全部通过。v0.7.0 在不增加新功能的前提下完成稳定版晋升。

## v0.7.0 完成标准

- 用户可以明确选择三种目标模式，默认不会移动影片。
- 预览中的所有目标路径与最终磁盘结果一致。
- 同卷、跨卷和 UNC 保存都不会覆盖目标影片，也不会在失败后丢失来源影片。
- 影片重命名在三种目标模式下均可独立启用或关闭。
- 完整自动化门禁、分阶段人工检查和 RC Jellyfin 验收全部通过。
