# v0.5.0-dev5 manual check

本轮只检查图片来源候选。建议复制一份测试影片，并使用 LibreDMM 与 R18.dev 都有图片的番号。自动化已经覆盖文件事务和影片内容不变，本轮无需重复计算影片 SHA-256。

## A01 — 默认图片候选

1. 保持“多来源搜索（推荐）”并搜索测试番号。
2. 查看底部新增的“图片来源”区域。

期望：

- “封套”列出 LibreDMM 与 R18.dev；默认选择当前合并资料所采用的封套来源，通常是 LibreDMM。
- “剧照”默认选择“合并去重”，并显示两个来源合并后的网址候选数量。
- 文本字段来源与图片来源互不影响。

## A02 — 封套切换与分辨率

1. 记录当前 poster 与 fanart 预览。
2. 把“封套”切换到另一个来源，等待加载完成。
3. 再切回第一个来源。

期望：

- poster 与 fanart 同时切换，因为两者来自同一张完整封套。
- 成功加载后，来源选项由“待检测”变为实际分辨率，例如 `800×538`。
- fanart 提示同时显示来源、完整封套和分辨率。
- 切回已加载来源时可以使用缓存，不出现重复失败弹窗。

## A03 — 剧照来源独立选择

1. 依次选择“合并去重”、LibreDMM、R18.dev。
2. 同时观察封套选择和文本字段。

期望：

- 每项显示候选张数。
- 切换剧照不会改变 poster/fanart，也不会改变标题、演员等文字。
- 状态栏提示保存时会按图片内容去重。

## A04 — 保存预览与实际输出一致

1. 选择一个非默认封套来源。
2. 剧照选择“合并去重”，勾选海报、fanart 与“保存全部剧照”。
3. 点击保存，先检查变更预览，再确认保存。

期望：

- 预览明确显示最终“封套来源”和“剧照来源”。
- 保存后的 poster 与 fanart 对应所选封套，不会因文字字段来源不同而改变。
- `extrafanart` 来自合并候选；内容相同但网址不同的图片只保留一份。
- 完成状态显示实际使用的两个图片来源。

## A05 — 单来源与失败提示

1. 分别使用显式 LibreDMM 和显式 R18.dev 搜索。
2. 如果某个真实候选恰好加载失败，保留失败现场并查看状态栏与日志；不需要人为断网制造失败。

期望：

- 单来源搜索只显示该来源的图片选项，选择框保持只读但信息可见。
- 明确选择的封套若失败，软件提示改选其他来源，不会静默换成另一个网站。
- “打开日志”可看到封套来源、实际地址与分辨率，以及剧照候选数和去重后的实际张数。

结果可按 `A01 PASS / FAIL` 至 `A05 PASS / FAIL` 回复；失败时请附界面截图、状态栏文字和相关日志行。
