# JavMetaLite v0.8.0-rc1 最终集成验收

状态：等待 R01–R05 人工验收。

本清单只验证 v0.8 新入口、安全偏好、最近目录与既有安全保存流程能在发布版中连贯工作。dev1–dev3 已分别通过的 E01–E08、P01–P02、H01–H03 不再逐项重复。

## 准备

1. 只使用一份测试影片副本，并记录其 SHA-256。
2. 准备两个已经存在的空文件夹作为自定义根目录；其中一个最好位于另一个本地盘符，没有第二个盘符时使用同盘目录即可。
3. 使用 LibreDMM 或 R18.dev 能找到的番号；如影片旁已有 NFO、poster 或 fanart，也记录这些 sidecar 的 SHA-256。

## R01 — 发布版启动与入口一致

1. 无参数启动 `publish-v0.8.0-rc1\JavMetaLite.exe`，确认底部显示 `v0.8.0-rc1 · 发布候选版`。
2. 关闭后，通过 Windows“打开方式”或带影片路径的命令行再次启动。
3. 确认影片、番号和已有本地 NFO/图片正常载入；启动本身没有联网搜索，也没有生成、移动或修改文件。

通过标准：发布版可用，启动载入与既有选择/拖入入口得到相同影片状态，且仅载入不写入。

## R02 — 安全偏好与最近目录重启

1. 勾选“记住保存偏好”，选择自定义目标模式，依次选择准备好的两个根目录，并设置自己常用的安全输出选项。
2. 可临时勾选“直接保存并覆盖（跳过预览）”，然后关闭并重新启动程序。
3. 检查目标模式、输出选项和最近目录是否恢复；确认“直接保存并覆盖（跳过预览）”仍为关闭。

通过标准：只有明确允许记忆的安全偏好与最近目录恢复，危险选项永远不跨启动保留。

## R03 — 不可用根目录隔离

1. 让当前自定义根目录暂时不可用，例如临时改名其中一个测试目录，或选择当前离线的盘符路径。
2. 从“最近目录”切换到该路径并尝试进入保存流程。
3. 恢复该目录前，检查磁盘上没有被程序自动创建同名目录，也没有新的 metadata 或临时文件。

通过标准：界面明确提示目标不可用并阻止保存；程序不创建根目录、不写入文件，最近目录管理仍可用。

## R04 — 真实自定义目标保存

1. 恢复一个已经存在的目标根目录，选择影片并搜索资料。
2. 选择“自定义根目录的番号文件夹”，按需要启用影片重命名，点击保存。
3. 在预览中核对最终影片、NFO、poster、fanart 与可选剧照路径，再确认执行。
4. 保存完成后检查目标目录、文件名和临时文件残留，并重新计算影片 SHA-256。

通过标准：所有输出位于预览所示的 `<目标根目录>\<番号>`；没有重复番号目录或 `.JavMetaLite-*.tmp`；影片 SHA-256 与准备阶段一致。若使用另一盘符，界面应显示安全复制与 SHA-256 校验。

## R05 — 本地重新载入与无变化保护

1. 重新打开目标目录中的影片，确认字段来自“本地 NFO”，poster/fanart 来自“本地图片”。
2. 搜索在线资料，确认新的非空在线文字默认显示，同时每个字段仍可切回本地 NFO 或手动值；本地图片仍可选择。
3. 不修改内容再次点击保存，检查预览，然后取消，或确认一次明确显示为“保持不变”的保存。
4. 重新计算未发生变化的影片及 sidecar SHA-256。

通过标准：本地资料可往返载入；搜索后的来源选择符合既有规则；无变化文件不被重写，影片和未变化 sidecar 哈希保持一致。

## 结果反馈

```text
R01 PASS/FAIL
R02 PASS/FAIL
R03 PASS/FAIL
R04 PASS/FAIL
R05 PASS/FAIL
影片 SHA-256 是否一致：YES/NO
无变化 sidecar 哈希是否一致：YES/NO
补充观察：
```

如果失败，请附对应步骤截图、状态栏文字，以及只包含该测试番号的相邻日志行。若看到 `.JavMetaLite-*.tmp`，请先保留现场，不要手动删除。
