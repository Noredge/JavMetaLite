# JavMetaLite v0.7.0-rc1 最终集成验收

这份清单验证 v0.7 的最终真实闭环：单部影片从来源盘整理到自定义目标盘，生成的 Jellyfin metadata 被正确识别，再由 JavMetaLite 安全载入。dev2 与 dev3 已覆盖布局、取消、目标冲突、同卷回归和跨盘 SHA-256，本清单不机械重复全部用例。

当前没有可用 UNC 环境，因此真实 UNC 执行不属于本轮必测项，也不影响 RC1 晋级。自动化门禁仍覆盖 UNC 路径规划、跨卷安全复制、取消、校验失败、晚到冲突和回滚；未来有真实共享时可以补测速度、权限与断线表现。

## 准备

1. 只使用影片副本，不选择唯一原件。
2. 在来源盘准备 `Incoming`，在另一个本地盘准备一个 Jellyfin 测试库根目录 `Library`。
3. 使用 LibreDMM 或 R18.dev 能找到的番号，并记录来源影片 SHA-256。
4. 在 Jellyfin 中添加或准备指向 `Library` 的测试媒体库。

## R01 — RC1 启动与安全默认

1. 启动 `publish-v0.7.0-rc1\JavMetaLite.exe`。
2. 确认底部显示 `v0.7.0-rc1 · 发布候选版`。
3. 确认默认目标是“影片所在位置（不移动）”，影片重命名和直接覆盖均默认关闭。
4. 确认多来源搜索、NFO、海报和 fanart 默认行为正常。

通过标准：版本正确、界面正常、安全默认未改变。

## R02 — 跨盘符最终预览

1. 选择 `Incoming` 中的测试影片并搜索资料。
2. 把目标位置设为“自定义根目录的番号文件夹”，选择另一个盘符的 `Library`。
3. 勾选“影片重命名为番号”。
4. 点击保存，核对“当前影片”“最终影片”、NFO、poster、fanart 与可选剧照路径。

通过标准：界面显示安全复制与 SHA-256 校验；预览中的影片动作为“复制并校验”；所有目标位于 `Library\<番号>`，没有额外重复的番号目录。

## R03 — 保存与磁盘结果

1. 确认 R02 的预览并等待保存完成。
2. 检查目标目录中的影片、NFO、poster、fanart 和所选剧照。
3. 确认来源影片仅在成功后消失，两边没有 `.JavMetaLite-*.tmp` 残留。
4. 记录目标影片、NFO、poster 与 fanart 的 SHA-256，供 R06 对比。

通过标准：目标 SHA-256 与准备阶段一致；文件名和 NFO 图片引用符合 Jellyfin 规范；没有半成品、重复影片或事务残留。

## R04 — Jellyfin 扫描

1. 在 Jellyfin 中扫描或刷新测试媒体库。
2. 打开影片详情，检查标题、原始标题、简介、演员、类型、poster 与背景 fanart。
3. 如保存了剧照，确认它们没有干扰主海报和背景图识别。

通过标准：Jellyfin 只识别一部影片，并显示 R03 的最终 metadata 与图片。

## R05 — JavMetaLite 再次载入

1. 在 JavMetaLite 中选择目标目录里的影片。
2. 确认字段从 `本地 NFO` 载入，poster/fanart 以 `本地图片` 显示。
3. 不修改内容，点击保存并检查预览。

通过标准：NFO 与图片显示“保持不变”，最终路径仍在自定义目标目录，没有意外回到来源盘。

## R06 — 无变化执行与最终哈希

1. 确认 R05 的无变化保存。
2. 重新计算影片、NFO、poster 与 fanart 的 SHA-256。
3. 再次刷新 Jellyfin。

通过标准：无变化 sidecar 不被重写；影片 SHA-256 仍与最初一致；Jellyfin 显示保持正确且没有重复条目。

## 结果反馈

```text
R01 PASS/FAIL
R02 PASS/FAIL
R03 PASS/FAIL
R04 PASS/FAIL
R05 PASS/FAIL
R06 PASS/FAIL
影片 SHA-256 是否一致：YES/NO
无变化 sidecar 哈希是否一致：YES/NO
补充观察：
```

如果失败，请附对应步骤截图、状态栏文字，以及只包含该测试番号的相邻日志行。若看到 `.JavMetaLite-*.tmp`，请先保留现场，不要手动删除。
