# v0.6.0-dev4 增量人工检查

## 本轮范围

本轮只检查已有 NFO、poster、fanart 的安全往返保存。dev3 的 A01–A05 已全部通过，不需要重复。请只使用测试副本，不要直接使用唯一媒体文件。

运行：

```text
publish-v0.6.0-dev4\JavMetaLite.exe
```

准备一个 `B01-RoundTrip` 测试目录，其中包含：

- `SNOS-255.mp4`
- `SNOS-255.nfo`
- `SNOS-255-poster.jpg`
- `SNOS-255-fanart.jpg`

建议先在 NFO 的 `<movie>` 内手动增加一行 `<my_custom_field owner="manual">必须保留</my_custom_field>`，并记录四个文件的 SHA-256：

```powershell
Get-FileHash "完整文件路径" -Algorithm SHA256
```

## B01 — 有效 NFO 可编辑并保留未知 XML

1. 选择 `B01-RoundTrip\SNOS-255.mp4`。
2. 确认 NFO 字段正常载入，“保存”按钮可用，提示说明可以保留未知 XML。
3. 只修改标题或简介中的一项，不切换图片来源。
4. 点击“保存”，确认预览中 NFO 标记为“更新”，poster 与 fanart 标记为“保持不变”。
5. 确认保存；用文本编辑器打开 NFO，检查修改后的字段和 `my_custom_field` 都存在。
6. 确认 poster、fanart 和影片 SHA-256 与保存前完全一致。

通过标准：受支持字段更新；未知 XML 保留；未选择替换的图片与影片均未改写。

## B02 — 取消预览零写入

1. 重新记录 NFO、poster、fanart 和影片的 SHA-256。
2. 在软件中再修改一个字段，点击“保存”。
3. 在变更预览窗口点击“取消”或关闭窗口。
4. 再次计算四个 SHA-256。

通过标准：四个哈希全部与取消前一致，目录中没有 `.JavMetaLite-*.tmp` 临时项目。

## B03 — 无变化时明确保持不变

1. 重新选择刚才的影片，不修改任何字段，也不切换图片来源。
2. 点击“保存”。
3. 检查预览中的 NFO、poster 与 fanart 都显示“保持不变”，不显示“更新”或“替换图片”。
4. 确认保存后再次检查三个 sidecar 的 SHA-256。

通过标准：软件报告没有需要写入的变更，三个 sidecar 均保持原字节。

## B04 — 在线或手动封套明确替换图片

1. 重新选择影片并搜索在线资料，或通过“选择本地完整封套…”选择一张真实横板封套。
2. 在“封套 / Fanart”菜单中选择这个在线或手动来源。
3. 保持“高清海报”和“横版 fanart”勾选，点击“保存”。
4. 检查预览中两张图片显示“替换图片”，NFO 显示“更新”。
5. 确认保存，检查新 poster/fanart 能正常打开，NFO 中的本地图片文件名正确，未知 XML 仍存在。
6. 确认影片 SHA-256 不变。

通过标准：只有明确选择的新完整封套替换图片；NFO 与图片引用一致；未知 XML 和影片受保护。

## B05 — 直接保存必须显式开启

1. 再复制一份 B01 目录作为独立测试副本。
2. 修改标题，勾选“直接保存并覆盖（跳过预览）”。
3. 点击“保存”。
4. 确认不出现变更预览，NFO 直接安全更新；取消勾选后再次修改并保存，应恢复显示预览。

通过标准：只有显式勾选时跳过预览；关闭后预览恢复；影片未被改写。

## B06 — 整理时迁移已知 sidecar

1. 使用一个文件名不是标准番号、但 NFO/poster/fanart 与影片同名的测试副本。
2. 勾选“整理到番号文件夹”和“影片重命名为番号”，保持“本地图片”为封套来源。
3. 保存前检查预览：影片显示移动/重命名，NFO 显示更新或保持迁移，poster/fanart 显示保持不变。
4. 确认后检查番号文件夹中是否只有新命名的影片和三个已知 sidecar；原位置不再残留旧名称。
5. 比较迁移前后的 poster、fanart 和影片 SHA-256；检查 NFO 内图片引用使用新文件名，未知 XML 仍存在。

通过标准：已知 sidecar 与影片一起迁移；图片和影片字节不变；旧文件安全退役。

## B07 — 外部修改冲突必须阻止

1. 选择一个带有效 NFO 的测试影片并在软件中修改标题，暂时不要保存。
2. 用文本编辑器在磁盘上的 NFO 里加入一行新注释并保存。
3. 回到软件点击“保存”并确认预览（如果可以进入）。
4. 检查软件是否拒绝继续，提示 NFO 已在载入后发生变化。
5. 确认文本编辑器写入的注释仍存在，影片和图片没有变化。

通过标准：外部修改不会被静默覆盖；要求用户重新选择影片以取得最新版本。

## 回报格式

```text
B01 PASS / FAIL：
B02 PASS / FAIL：
B03 PASS / FAIL：
B04 PASS / FAIL：
B05 PASS / FAIL：
B06 PASS / FAIL：
B07 PASS / FAIL：
影片 SHA-256 是否一致：YES / NO
补充观察：
```

## 验收结果（2026-08-16）

- B01 PASS
- B02 PASS
- B03 PASS
- B04 PASS
- B05 PASS
- B06 PASS
- B07 PASS
- 影片 SHA-256 一致：YES

结论：有效 NFO 的选择性更新与未知 XML 保留、取消预览零写入、无变化不重写、显式图片替换、直接保存开关、已知 sidecar 整理迁移和外部修改冲突保护均通过人工检查。结合完整自动化门禁，`v0.6.0-dev4` 验收完成，可以进入 RC 阶段。
