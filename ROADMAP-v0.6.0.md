# JavMetaLite v0.6.0 路线：本地 metadata 再编辑闭环

## 目标

让 JavMetaLite 不仅能为新影片生成 metadata，也能安全地重新打开并编辑自己或 Jellyfin 已经使用的本地资料：

```text
选择单个影片
→ 检测同目录本地 NFO 与图片
→ 载入为“本地资料”候选
→ 按需搜索 LibreDMM / R18.dev / JAVLibrary
→ 用户逐字段检查、切换或手动修改
→ 保存前预览
→ 安全更新本地 NFO 与图片
```

产品定位保持不变：单片、可检查、可撤销、Jellyfin 兼容。

## 明确不做

- 不扫描媒体库，不加入批量刮削。
- 不自动整理未知字幕、预告片或其他伴随文件。
- 不写入 MP4/MKV 容器内部 metadata。
- 不在本版本增加更多在线刮削器或中文翻译来源。
- 不恢复独立 poster/fanart 来源，也不跨来源合并剧照。
- 不生成演员图片文件夹。

## 核心行为决定

### 1. 本地资料优先保护

- 首阶段只识别与影片同名的 `<影片名>.nfo`。
- 找到有效 NFO 时，自动载入已支持字段，并将来源显示为“本地 NFO”。
- 本地值作为当前默认值；随后进行在线搜索时，只增加候选，不静默覆盖已有本地选择。
- 没有本地 NFO 时，保持 v0.5 的现有搜索流程。

### 2. 安全解析与往返保存

- 使用禁用 DTD/外部实体的 XML 读取设置，拒绝无法安全解析或根元素不是 `<movie>` 的文件。
- 读取当前支持的 title、originaltitle、id、日期、时长、studio、director、plot、rating、genre、actor、Label 和 Series。
- 保存已有 NFO 时，只更新 JavMetaLite 管理的字段；未知元素、未知属性和其他软件写入的数据必须保留。
- 原文件继续经过现有暂存、备份、提交与回滚流程，绝不原地半写入。
- 解析失败只显示可理解的提示并写日志，不自动重建或覆盖原 NFO。

### 3. 本地图片作为同一封套来源

- 后续阶段检测 `<影片名>-poster` 与 `<影片名>-fanart` 的常见图片扩展名。
- 两张本地图片共同显示为一个“本地图片”来源；缺少其中一张时明确标记，不伪造另一张。
- 用户可以选择一张本地完整封套，由软件继续统一生成 poster 与 fanart。
- 手动图片只影响当前影片，不建立全局素材库。

### 4. 兼容 v0.5 候选模型

- “本地 NFO”进入现有逐字段候选菜单，而不是另建第二套编辑界面。
- LibreDMM 与 R18.dev 仍各请求一次，并保留独立来源快照。
- 手动编辑候选仍可恢复。
- 封套来源仍只有一个下拉，poster 与 fanart 始终锁定在一起。

## 分阶段路线

### dev1 — 本地 NFO 只读基座

只修改 Core，不接入主界面：

- 新增本地 sidecar 路径检测。
- 新增安全 NFO 解析器与“本地 NFO”来源快照。
- 支持当前 `MovieMetadata` 字段以及演员图片 URL。
- 保存原始 XML 文档或等价保留信息，为后续往返写入做准备。
- 不修改、移动或创建任何真实文件。

完成门槛：离线测试覆盖有效 NFO、缺失字段、多值 genre/actor、Label/Series、无效 XML、错误根元素、DTD/外部实体拒绝和未知元素保留。

### dev2 — 主界面载入本地资料

- 选择影片时自动检测同名 NFO。
- 有效本地资料直接显示，并在状态栏明确说明来源与路径。
- 在线搜索后，本地值、LibreDMM、R18.dev 与手动值可以在同一字段菜单切换。
- 选择新影片或重新搜索时不会残留旧候选。

完成门槛：UI smoke 覆盖本地来源标记、默认保留本地值、在线候选加入和手动值恢复；人工检查不要求保存。

### dev3 — 本地图片与手动封套

- 检测并预览现有 poster/fanart。
- 把本地图片加入统一封套来源菜单。
- 增加“选择本地完整封套”入口，由同一张封套生成 poster 与 fanart。
- 图片无效时只忽略该候选并写日志，不影响 NFO 编辑。

完成门槛：自动化使用合成图片验证格式、尺寸与来源锁定；人工检查 Windows 文件选择和实际视觉质量。

### dev4 — 往返保存与变更预览

- 在已有 NFO 上更新受支持字段，同时保留未知 XML。
- 预览区区分“创建”“更新”“保留不变”和“替换图片”。
- 取消预览不产生文件变化；直接覆盖仍必须显式开启。
- 保存失败时恢复原 NFO 与图片。

完成门槛：逐字节备份恢复、未知节点保留、取消零写入、冲突阻止和影片 SHA-256 不变全部进入回归测试。

### RC — 真实 Jellyfin 往返验收

- 打开一个已被 Jellyfin 正常识别的影片目录。
- 载入本地 NFO，切换少量在线字段并替换封套。
- 保存、重新扫描 Jellyfin，再次用 JavMetaLite 打开。
- 确认选择值、未知字段、图片和影片哈希全部保持正确。

## dev1 建议的数据边界

建议新增以下职责，而不立即改变 UI：

```text
LocalSidecarLocator
  输入：影片路径
  输出：同名 NFO、poster、fanart 的候选路径

NfoReader
  输入：NFO 路径或只读流
  输出：MovieMetadata + 原始 XML 保留上下文

LocalMetadataBundle
  包含：本地来源快照、sidecar 路径、原始文档信息与诊断
```

`NfoWriter` 在 dev1 保持原样；往返更新由 dev4 在测试充分后接入，避免读取与覆盖同时进入主流程。

## 发布与维护策略

- `v0.5.x` 只接收来源网站变更、解析错误与安全性修复。
- `feature/v0.6.0` 从 `v0.5.0` 标签开始，阶段提交必须保持完整自动化门槛通过。
- 每个 dev 阶段只安排对应的增量人工检查；稳定版前再执行一次完整集成验收。
