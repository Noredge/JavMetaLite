# v0.5.0-dev5-r2 manual check

本轮只检查左侧统一封套来源下拉。建议使用 LibreDMM 与 R18.dev 都有完整封套的测试番号。

## C01 — 左侧下拉结构

1. 使用“多来源搜索（推荐）”搜索测试番号。
2. 查看 poster 与 fanart 之间的“封套 / Fanart”区域。
3. 点击蓝色来源标记。

期望：

- 来源标记与右侧文字字段标记风格一致，显示当前来源及 `▾`。
- 下拉菜单为完整深色结构，不出现白色边栏。
- 菜单列出 LibreDMM 与 R18.dev，并明确说明 poster / fanart 共用。
- 没有剧照来源或“合并去重”选项。

## C02 — 两个预览同步切换

1. 记录当前 poster 与 fanart。
2. 在“封套 / Fanart”菜单中选择另一个来源。
3. 再切回第一个来源。

期望：

- poster 与 fanart 每次同时刷新，来源标记同步更新。
- 不会出现 poster 使用 LibreDMM、fanart 使用 R18.dev 的组合。
- 切换封套不会改变标题、演员、简介等文字字段，也不会改变剧照集合。

## C03 — 保存输出一致

1. 选择一个非默认封套来源。
2. 勾选海报、fanart；如需验证剧照隔离，也可勾选“保存全部剧照”。
3. 保存到测试目录并查看输出。

期望：

- `<影片名>-poster.jpg` 与 `<影片名>-fanart.jpg` 都来自所选封套来源。
- `extrafanart` 仍来自本次整体搜索结果，不会因封套下拉而跨来源合并或切换。
- 原影片内容没有被修改。

结果可按 `C01 PASS / FAIL` 至 `C03 PASS / FAIL` 回复；失败时请附左侧卡片截图与状态栏文字。
