# JavMetaLite v0.4.0-rc1 人工验收

这份清单只覆盖必须依赖真实网站、Windows 文件环境、Jellyfin 或人工视觉判断的部分。自动化发布门槛由 `scripts/Test-Automated.ps1` 负责。

## 验收原则

- 只使用影片副本，绝不选择唯一原件。
- 所有测试文件放在一个独立测试根目录，例如 `D:\JavMetaLite-RC1-Test`。
- 每个用例使用独立子目录，不复用已经被其他用例修改过的输入文件，除非步骤明确要求。
- 发现影片丢失、哈希改变或其他文件被修改时立即停止，不继续后续用例。
- 失败时不要清理现场；保留相关目录、截图和日志，先把用例编号与现象告诉开发者。

## 0. 准备工作

### 0.1 建立测试目录

在空间足够的磁盘建立：

```text
JavMetaLite-RC1-Test/
  M02-LibreDMM/
  M03-R18/
  M04-JAVLibrary/
  M05-Overwrite/
  M06-Conflict/
  M07-Logs/
  M08-Reopen/
  Jellyfin-Library/
```

### 0.2 准备影片副本

选择至少三部你确认能够正常播放的影片，各复制一份到测试目录：

| 代号 | 用途 | 建议条件 |
| --- | --- | --- |
| A | LibreDMM | 网页有简介、完整封套和 Sample Images |
| B | R18.dev | R18.dev 有英文标题和 Gallery |
| C | JAVLibrary | JAVLibrary 能找到详情页 |

将 A 再复制到 `M05-Overwrite`，将 B 再复制到 `M06-Conflict`。不要使用硬链接；使用普通复制。

### 0.3 记录影片哈希

对测试副本运行以下 PowerShell 命令，把结果保存在验收记录中：

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath "D:\JavMetaLite-RC1-Test\M02-LibreDMM\你的影片.mp4"
```

MKV、AVI 或 WMV 只需替换文件路径。移动或重命名后，再对新路径执行同一命令；哈希必须完全一致。

### 0.4 准备 Jellyfin 测试库

在 Jellyfin 中建立独立电影媒体库，路径指向：

```text
D:\JavMetaLite-RC1-Test\Jellyfin-Library
```

不要把测试目录加入正式媒体库。

## M01 — RC1 启动和初始状态

1. 双击 `publish-v0.4.0-rc1\JavMetaLite.exe`。
2. 确认窗口底部显示 `v0.4.0-rc1 · 人工验收候选版`。
3. 确认以下默认状态：
   - 生成 NFO：开启。
   - 高清海报：开启。
   - 横版 fanart：开启。
   - 保存全部剧照：关闭。
   - 直接保存并覆盖：关闭。
   - 整理到番号文件夹：关闭。
   - 影片重命名为番号：关闭。
4. 点击来源选择框，确认文字清晰可读。
5. 暂时不要保存任何文件。

预期结果：程序正常启动，没有错误弹窗，所有默认选项与上述一致。

## M02 — LibreDMM、取消预览和原位输出

输入：影片 A 的副本。

1. 把影片拖入窗口或点击“选择影片”。
2. 确认番号识别正确；必要时手动修正。
3. 来源选择 `LibreDMM`，点击“搜索资料”。
4. 检查标题、日期、时长、片商、导演、演员、类型和简介。
5. 视觉检查 poster 与完整横版 fanart，确认二者不是同一张竖图。
6. 勾选“保存全部剧照”。
7. 保持两项整理选项和“直接保存并覆盖”关闭。
8. 点击“保存”，确认出现变更预览，而且没有移动或重命名影片的项目。
9. 第一次点击取消，使用文件资源管理器确认没有生成 NFO、poster、fanart 或 `extrafanart`。
10. 再次点击“保存”，确认预览并执行。
11. 对保存后的影片重新计算 SHA-256。

预期结果：

- 影片保持原路径和原文件名，哈希不变。
- 同目录生成同名 NFO、poster、fanart 和 `extrafanart`。
- 简介不是空白，fanart 是完整封套，样张可正常打开。

## M03 — R18.dev 英文资料与推荐整理布局

输入：影片 B 的副本。

1. 选择影片，来源选择 `R18.dev` 并搜索。
2. 确认“标题”主要为英文，“原始标题”为日文。
3. 检查英文演员、导演、片商和类型是否合理。
4. 检查 poster、完整封套 fanart 和 Gallery 样张。
5. 开启“保存全部剧照”。
6. 开启“整理到番号文件夹”和“影片重命名为番号”。
7. 保持“直接保存并覆盖”关闭，点击“保存”。
8. 在预览中核对目标结构应为：

```text
原影片目录/
  番号/
    番号.ext
    番号.nfo
    番号-poster.jpg
    番号-fanart.jpg
    extrafanart/
```

9. 确认执行，再对移动后的影片计算 SHA-256。

预期结果：影片移动并重命名到番号子目录，哈希不变；NFO 保留英文标题和日文原始标题。

## M04 — JAVLibrary 浏览器辅助导入

输入：影片 C 的副本。

1. 选择影片并确认番号。
2. 来源选择 `JAVLibrary`，点击“搜索资料”。
3. 如果网站要求浏览器验证，确认内置浏览器窗口确实出现并完成验证。
4. 在详情页加载完成后使用“网页导入”。
5. 检查导入的标题、日期、演员、类型和封面。
6. 只开启“生成 NFO”，关闭三种图片输出与两项整理选项。
7. 保存并确认预览。

预期结果：能够从已打开的详情页导入资料并生成有效 NFO；不生成图片，也不移动影片。

## M05 — 直接覆盖必须跳过预览

输入：影片 A 在 `M05-Overwrite` 中的独立副本。

1. 使用 LibreDMM 搜索并按默认输出保存一次。
2. 在软件中把标题末尾临时加上 ` RC1-OVERWRITE-TEST`。
3. 勾选“直接保存并覆盖（跳过预览）”。
4. 点击“保存”。
5. 观察是否直接开始保存；整个过程中不应出现“保存前变更预览”。
6. 用文本编辑器打开 NFO，确认测试文字已经写入。
7. 取消勾选“直接保存并覆盖”，避免影响后续用例。

预期结果：metadata 被覆盖，预览不出现；影片路径、名称和哈希不变。

## M06 — 目标影片冲突必须硬性阻止

输入：影片 B 在 `M06-Conflict` 中的独立副本。

1. 记下识别出的标准番号和影片扩展名。
2. 在影片当前目录内手动建立同名番号子目录。
3. 向子目录复制任意一个测试影片副本，并命名为 `番号.ext`，形成目标影片冲突。
4. 在软件中选择原来的输入影片并搜索资料。
5. 同时开启两项整理选项和“直接保存并覆盖”。
6. 点击“保存”。

预期结果：

- 软件显示目标影片已存在并阻止执行。
- 即使开启直接覆盖，也不能覆盖目标影片。
- 输入影片和冲突影片都仍然存在，二者哈希均未改变。
- 不应在目标目录产生新的 NFO 或图片。

完成记录后可以删除整个 `M06-Conflict` 测试目录。

## M07 — 来源失败和日志可诊断性

1. 选择任意测试影片。
2. 暂时把番号改成明显不存在的值，例如 `ZZZZ-000`。
3. 来源选择“自动补全（推荐）”，点击“搜索资料”。
4. 等待失败提示，然后点击底部“打开日志”。
5. 打开当天日志，搜索 `ZZZZ-000`。

预期结果：

- 软件给出可理解的失败信息，没有崩溃。
- 日志包含番号、尝试过的来源以及失败原因。
- 搜索失败不会生成或修改本地 metadata。

## M08 — 再次打开已整理影片不得重复嵌套

输入：M03 已整理完成的 `番号\番号.ext`。

1. 重新启动软件并选择该影片。
2. 搜索资料。
3. 开启“整理到番号文件夹”和“影片重命名为番号”。
4. 保持直接覆盖关闭，点击“保存”。
5. 检查预览中的目标路径。
6. 取消保存即可，不需要再次覆盖。

预期结果：目标仍是当前番号目录，不得计划生成 `番号\番号\番号.ext`。

## M09 — Jellyfin 首次导入

1. 把 M02 和 M03 的完整影片目录复制到 `Jellyfin-Library`。
2. 在 Jellyfin 中扫描测试媒体库。
3. 分别打开两部影片，检查：
   - 标题和原始标题。
   - 日期、时长、简介。
   - 演员、类型和片商。
   - poster 与背景 fanart。
   - Sample Images 是否作为额外背景可用。
4. 确认每个目录只识别为一部影片，没有重复条目。

预期结果：Jellyfin 使用本地 NFO 和图片，字段与软件保存内容一致。

## M10 — Jellyfin 覆盖更新

1. 在 Jellyfin 测试库内选择其中一部影片文件，用 JavMetaLite 打开。
2. 在标题末尾加入 ` RC1-JELLYFIN-UPDATE`。
3. 勾选“直接保存并覆盖”，保存。
4. 在 Jellyfin 中重新扫描该影片；如果仍显示缓存内容，再执行该影片的 metadata 刷新。
5. 检查新标题是否出现，同时 poster 和 fanart 仍正常。

预期结果：Jellyfin 能读取覆盖后的 NFO，不产生重复影片或丢失图片。

## 停止测试的条件

出现以下任何情况时停止后续测试并保留现场：

- 影片 SHA-256 改变。
- 原影片和目标影片同时丢失。
- 目标影片冲突时发生覆盖。
- 取消预览后仍产生文件变化。
- 失败后出现无法解释的 `.JavMetaLite-*.tmp` 目录。
- Jellyfin 把一个目录识别为多个影片。

## 结果记录

执行后把状态改为 `PASS`、`FAIL` 或 `BLOCKED`，并记录截图或说明：

| 用例 | 状态 | 证据或备注 |
| --- | --- | --- |
| M01 RC1 启动 | 未执行 | |
| M02 LibreDMM 与取消预览 | 未执行 | |
| M03 R18.dev 与推荐整理 | 未执行 | |
| M04 JAVLibrary 导入 | 未执行 | |
| M05 直接覆盖 | 未执行 | |
| M06 影片冲突 | 未执行 | |
| M07 日志诊断 | 未执行 | |
| M08 防止重复嵌套 | 未执行 | |
| M09 Jellyfin 首次导入 | 未执行 | |
| M10 Jellyfin 覆盖更新 | 未执行 | |

最简反馈格式：

```text
M01 PASS
M02 PASS
M03 FAIL：预览中的目标路径为……
```

发生失败时请附上截图，并复制日志中与该番号相关的几行；不要发送整份包含其他路径信息的日志。
