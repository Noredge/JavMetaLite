# JavMetaLite v0.7.0-rc2 集中复测

RC1 的目标路径、跨盘事务与 Jellyfin R01–R06 已全部通过。RC2 只复测一个行为修正：影片刚载入时显示本地 NFO，搜索成功后默认选择新的在线文字资料，同时保留本地和手动候选。请使用带本地 NFO、poster 与 fanart 的测试影片副本。

## C01 — 搜索前仍以本地为准

1. 启动 RC2 并选择带有现有 NFO 的影片。
2. 不点击搜索，检查标题、简介等字段及右上角来源。

预期：字段显示本地 NFO 内容，来源为“本地 NFO”；不会自动写入或替换任何文件。

## C02 — 搜索后默认显示新资料

1. 选择“多来源搜索（推荐）”并点击搜索。
2. 检查标题、简介、演员、导演、片商和类型等在线有值的字段。

预期：在线返回的非空字段自动显示新值，来源标记为 LibreDMM 或 R18.dev；不再继续默认显示旧的本地值。某个在线来源没有返回的字段可以继续显示“本地 NFO”。

## C03 — 本地与在线来源可往返

1. 打开一个同时有本地与在线值的字段右上角菜单。
2. 切换到“本地 NFO”，再切回 LibreDMM 或 R18.dev。

预期：候选和值均完整存在，切换立即生效；即使新旧文本相同，搜索后默认来源仍应标记为在线来源。

## C04 — 搜索前手动值仍可恢复

1. 重新选择影片，在搜索前手动修改一个在线也有值的字段。
2. 执行搜索，然后打开该字段来源菜单。
3. 选择“手动编辑”，再切回在线来源。

预期：搜索后默认显示在线值；搜索前手动值仍在候选菜单中并可恢复。

## C05 — 本地封套不自动改变

- 在上述搜索前后检查左侧 poster/fanart 与“封套 / Fanart”来源。

预期：已有本地 poster/fanart 继续显示，来源保持“本地图片”；在线封套只作为可选候选，不因文字搜索自动替换。

## C06 — 保存新默认值

1. 保持一个或多个在线文字字段为当前选择，点击保存并检查变更预览。
2. 确认保存后重新载入影片，并计算影片 SHA-256。

预期：NFO 写入预览中选择的在线文字值；再次载入后这些值成为新的本地 NFO；影片 SHA-256 不变，目标路径与图片没有意外变化。

## 回报格式

```text
C01 PASS/FAIL
C02 PASS/FAIL
C03 PASS/FAIL
C04 PASS/FAIL
C05 PASS/FAIL
C06 PASS/FAIL
影片 SHA-256 是否一致：YES/NO
```

如果失败，请附发生问题的字段、右上角来源菜单截图和相邻日志行。
