# v0.7.0-dev1 人工检查说明

dev1 只建立 Core 目标路径模型与自动化回归，不把自定义目标路径接入主界面，也不修改现有保存执行流程。因此本阶段没有新增的用户操作验收项。

可选快速检查：

- 启动打包程序，确认底部版本为 `v0.7.0-dev1 · 目标路径基座`。
- 选择影片后确认 v0.6 的默认行为保持不变：“整理到番号文件夹”和“影片重命名为番号”仍默认关闭。
- 不需要使用真实影片执行保存；自定义根目录、跨盘符与 UNC 规划由离线测试覆盖。

dev2 接入目标位置控件后再提供正式的人工交互清单。
